services:
  torrent-priority-manager:
    image: torrent-priority-manager:latest
    container_name: torrent-priority-manager
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
